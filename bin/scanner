#!/bin/sh
server=127.0.0.1:9000
grpc_cli ls $server proto.ScannerService
grpc_cli ls $server proto.ScannerService -l
grpc_cli type $server proto.ParamsScannerRequest

if [ $# -eq 0 ]; then
    exit 1
fi
action=$1
shift
grpc_cli call $server proto.ScannerService.$action $@
